<!-- Never rendered: the load in +page.ts redirects first. SvelteKit still
     requires this file for the route to exist.
     Nunca se renderiza: el load de +page.ts redirige antes. SvelteKit igual
     necesita este archivo para que la ruta exista. -->
